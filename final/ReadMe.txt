Files for the final concert.

Finchesizer
Development -- Markov chain from the presentation.
LF square noise passed through a ringing filter. Recent email.


To do:
    Make sure the variable namespaces don't conflict.
        Don't use single character variables.
        Only use clear, unambiguous tilde-prefixed names.
    Test finchesizer.scd with the iPad controller!
    Remove the Task() call at the end of finchesizer.scd?
    Some silent responses at the beginning of develop.
        No silent responses at the end.
